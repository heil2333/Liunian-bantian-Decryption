# Liunian-bantian-Decryption
Decrypt the secret language file of uid8114565
# 用于解包uid8114565游戏的脚本。

## 首次部署

所有脚本文件需要在同一文件夹下。

需要安装以下依赖：

· Python（用于解密）
· pip 需要安装依赖 pyperclip
  ```bash
  pip install pyperclip
  ```
· 7z 确保能在终端打开

首次使用需要设置文件 2 和 3。

文件 2 需要设置文件目录

```batch
:: 设置变量解压缩目录
set "CACHE_DIR=F:\Zipcache"
:: 设置变量归类已完成的文件
set "DONE_DIR=F:\down\#LinkCache"
```

文件 3 需要设置文件目录

```batch
:: 配置参数解压游戏目录
set "target_dir=F:\down\FirstPlay"
:: 配置参数解压缓存文件路径
set "cache_dir=F:\Zipcache"
```

## 食用方法

兽语解密

1. 去 UP 主的评论区复制兽语
2. 双击运行【1提取密文至txt剪贴板.bat】

示例：

剪贴板内容：者音兽兽兽兽音音音音兽音兽音音译音译音译兽译音者音者译者兽者音兽音兽者译

控制台返回结果：

```
========================================
      兽语解密工具 - 循环运行
========================================
从剪贴板读取的内容长度: 36
前5个字符: 者音兽兽兽
后5个字符: 兽音兽者译
提取的字典: ['兽', '音', '译', '者']
期望的前缀: 者音兽, 后缀: 译
实际的前缀: 者音兽, 后缀: 译

解密结果:
1234

按Enter键退出...
```

链接文件提取

将下载好的文件拖拽到【2提取连接exe至txt剪贴板.bat】，你会获得游戏本体的下载链接。

密码和二次解压

将下载好的游戏文件拖拽到【3拖拽解压缩HYBHL.bat】，游戏本体文件将会解压到设置的目录下：

```batch
:: 配置参数解压游戏目录
set "target_dir=F:\down\FirstPlay"
```
